# 学生110事件数据处理系统 - 离线部署与启动手册

本文档详细说明了如何在无互联网连接的离线环境下，使用 Python 虚拟环境部署并启动本系统。

## 1. 环境准备
请确保目标服务器或电脑上已正确安装 Python 环境。

## 2. 创建与激活虚拟环境
为了隔离项目依赖，不污染操作系统的全局 Python 环境，强烈建议使用虚拟环境（venv）进行部署。

### 第一步：进入项目目录
打开终端（CMD 或 PowerShell），使用 `cd` 命令进入到解压后的项目根目录下。

### 第二步：创建虚拟环境
在项目根目录下执行以下命令，这会在当前目录下生成一个名为 `venv` 的文件夹：
```bash
python -m venv venv
```

### 第三步：激活虚拟环境
根据您的操作系统，执行对应的激活命令：
- **Windows 系统**:
  ```bash
  venv\Scripts\activate
  ```
- **Linux / macOS 系统**:
  ```bash
  source venv/bin/activate
  ```
> **💡 提示**：激活成功后，您的命令行提示符（光标最前方）会出现 `(venv)` 字样，代表您已成功进入虚拟环境。

## 3. 离线安装依赖
本项目已经预先将所有依赖包提取并保存在了 `offline_packages` 文件夹中。
在**确保持续处于 (venv) 虚拟环境激活状态**下，直接运行以下命令即可实现完全断网的离线极速安装：

```bash
pip install --no-index --find-links=offline_packages -r requirements.txt
```
> **参数说明**：
> - `--no-index`：强制要求 pip 忽略默认的联网包索引（完全不联网）。
> - `--find-links=offline_packages`：指定从本地离线包文件夹中寻找并安装依赖。

## 4. 启动系统服务
依赖安装完成之后，依然在激活的虚拟环境中，执行以下命令即可启动 Web 服务：

```bash
python run.py
```

服务启动后，控制台会输出网络地址信息。
请打开现代浏览器（如 Chrome、Edge 等），访问以下地址即可进入系统：
🌐 **http://127.0.0.1:5000**

## 5. 附录：系统说明
- **默认管理员账号**：`admin`
- **默认管理员密码**：`123456`
- **数据库说明**：本项目使用轻量级 SQLite 数据库。首次运行 `run.py` 时，系统会自动在 `instance` 目录下生成 `student110.sqlite` 文件并初始化默认管理员账号。**如需清空整个系统数据，只需删除该 `.sqlite` 文件并重启服务即可。**
- **退出虚拟环境**：当您不再需要运行该系统，或想要退出虚拟环境时，直接在终端中输入并执行 `deactivate` 命令即可。
